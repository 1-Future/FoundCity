chore: npc_info stays in SKIP_TYPES; movement-only player_info skipped

npc_info content is position-dependent: which NPCs appear within 15 tiles
varies based on where the player walked. FC and REF use different pathfinders
(TS BFS vs WASM rsmod), so FC players land at slightly different tiles and
different NPCs enter/leave view range, producing false positional diffs.
NPC AI logic is identical between servers. Re-enable once pathfinding is fixed.

Also in this commit:
- Skip movement-only player_info in bucket builder (no masks = walk step,
  path non-deterministic across pathfinder implementations)
- Move if_close to SKIP_TYPES + PRESENCE_ANY_TICK global check (timing
  varies by 1 tick depending on when player reaches the NPC)
- Best-match diffTick for duplicate message types within same bucket
