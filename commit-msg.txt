fix: deterministic attack-rat test via tele + staffModLevel grant

Problems fixed:
Test 03 was targeting nid=1 at world pos (1884,4815) far from spawn.
Player walked 25+ tiles through NPC-dense terrain causing path divergence
between servers due to wandering NPCs occupying different tiles each run.
staffModLevel defaulted to 0, blocking tele and cheat commands even
with NODE_ALLOW_CHEATS=true.

Changes:
PlayerLoading.load(): set staffModLevel=4 when NODE_ALLOW_CHEATS=true.
Applied post-load so it overrides both new players and existing saves.
Test 03: tele player to (3198,3204) which is 1 tile east of rat nid=4759
spawn at (3197,3204). Path to rat is at most 2-3 tiles, no long walk.
Attack nid=4759 (type 47 Rat, 2HP) which spawns 5 tiles from player spawn.
listenMs bumped to 10*TICK for walk and combat resolution.
